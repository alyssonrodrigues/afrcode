const drawerWidth = 240

const styles = theme => ({
  menuList: {
    width: '100%',
    maxWidth: 360,
    backgroundColor: theme.palette.background.paper
  },
  drawerPaper: {
    position: 'relative',
    width: drawerWidth
  }
})

export default styles
